DVS-Gesture下面有Full和Static 和Baseline，分别代表在ablation对应着
ablation中提到的几个模型。 再次强调Full和Static都是MorphSNN，Static
是MorphSNN beta=1的版本， 也就是没有STSP操作， 为了加快训练时间（没有STSP
就可以使用并行架构计算，不用分开来），所以用了两套代码，但是本质上都是一回事。


Baseline 就是CogniSNN里面的连接方式，为了公平起见，我们没有用到CogniSNN
中提到的OR residual connection，这是因为MorphSNN中也没有用到任何的残差结构。

Static里面的M，beta等超参数写死了。baseline里面的节点是5是因为在构建graph时
会给他加一个start node和end node。


一般情况来说，下载好所有的额外包，只需要更换你的datasets目录就run train.py文件就
可以运行对应的代码。


OoD中的In-DVSGesture是以DVSGesture数据集作为In distribution, 然后分别以
CIFAR10-DVS、NCaltech101、 DVS-Lip做Out of distribution。只需要更改
模型权重的和数据集的目录就好。


同样的Robustness中对应着实验中的robustness章节